# ============================================
# INSTALLATION INSTRUCTIONS
# ============================================
#
# PREREQUISITES (run as root/sudo):
# sudo apt-get install -y libavformat-dev libavcodec-dev libavdevice-dev \
#                         libavutil-dev libavfilter-dev libswscale-dev \
#                         libswresample-dev pkg-config
#
# INSTALLATION ORDER (important for avoiding Cython compatibility issues):
#   1. pip install av==11.0.0  (newer version compatible with modern Cython)
#   2. pip install --no-deps git+https://github.com/myshell-ai/OpenVoice.git
#   3. pip install -r requirements.txt
#
# WHY THIS ORDER:
# - OpenVoice's setup.py specifies av==10.* which has Cython compilation errors
# - Installing av==11.0.0 first prevents the broken version from being installed
# - Installing OpenVoice with --no-deps prevents it from overwriting av
# - Then install the remaining dependencies from this file
#
# ============================================

# ============================================
# Core Dependencies
# ============================================
playwright>=1.49.0
aiofiles>=24.1.0
# Note: asyncio is part of Python stdlib, no need to install

# ============================================
# Azure OpenAI (GPT-5 Nano)
# ============================================
openai>=1.55.0
python-dotenv>=1.0.0

# ============================================
# Audio Processing
# ============================================
av==11.0.0  # CRITICAL: Use 11.0.0, not 10.* (Cython compatibility fix)
soundfile>=0.12.1
pydub>=0.25.1

# ============================================
# Video Processing & Analysis
# ============================================
opencv-python>=4.10.0

# ============================================
# Web Scraping & Parsing
# ============================================
beautifulsoup4>=4.12.0
lxml>=5.3.0
requests>=2.32.0

# ============================================
# Data Handling
# ============================================
pydantic>=2.10.0
pyyaml>=6.0.0

# ============================================
# Repository Analysis
# ============================================
GitPython>=3.1.0
toml>=0.10.0

# ============================================
# Utilities
# ============================================
tqdm>=4.67.0
colorama>=0.4.6

# ============================================
# PyTorch (for OpenVoice V2)
# ============================================
torch>=2.0.0
torchaudio>=2.0.0

# ============================================
# OpenVoice V2 Dependencies
# ============================================
# Note: OpenVoice itself is installed separately with --no-deps
# These are the dependencies OpenVoice needs:
librosa==0.9.1
faster-whisper==0.9.0
ctranslate2>=3.17,<4  # Required by faster-whisper
wavmark==0.0.3
eng_to_ipa==0.0.2
inflect==7.0.0
unidecode==1.3.7
whisper-timestamped==1.14.2
pypinyin==0.50.0
cn2an==0.5.22
jieba==0.42.1
gradio==3.48.0
langid==1.1.6

# ============================================
# Testing
# ============================================
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ============================================
# NOTES
# ============================================
# - numpy will be downgraded to 1.26.4 from 2.0.1 for librosa compatibility
# - See requirements-openvoice-working.txt for detailed installation notes
# - See requirements-no-openvoice.txt for installation without voice cloning
