# Working OpenVoice V2 Requirements
# This file documents the successful installation approach for OpenVoice V2
#
# PREREQUISITES (system packages):
# sudo apt-get install -y libavformat-dev libavcodec-dev libavdevice-dev
#                         libavutil-dev libavfilter-dev libswscale-dev
#                         libswresample-dev pkg-config
#
# INSTALLATION APPROACH:
# Due to av package Cython compatibility issues, install in this order:
#   1. pip install av==11.0.0  (newer version compatible with modern Cython)
#   2. pip install --no-deps git+https://github.com/myshell-ai/OpenVoice.git
#   3. pip install -r requirements-openvoice-working.txt

# Core dependencies (unchanged from requirements-no-openvoice.txt)
playwright>=1.49.0
aiofiles>=24.1.0

# Azure OpenAI
openai>=1.55.0
python-dotenv>=1.0.0

# Audio processing
av==11.0.0  # IMPORTANT: Use 11.0.0, not 10.* (Cython compatibility)
soundfile>=0.12.1
pydub>=0.25.1

# Video processing
opencv-python>=4.10.0

# Web scraping & parsing
beautifulsoup4>=4.12.0
lxml>=5.3.0
requests>=2.32.0

# Data validation
pydantic>=2.10.0
pyyaml>=6.0.0

# Repository management
GitPython>=3.1.0
toml>=0.10.0

# UI & Progress
tqdm>=4.67.0
colorama>=0.4.6

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# OpenVoice V2 dependencies (installed separately after av)
librosa==0.9.1
faster-whisper==0.9.0
wavmark==0.0.3
eng_to_ipa==0.0.2
inflect==7.0.0
unidecode==1.3.7
whisper-timestamped==1.14.2
pypinyin==0.50.0
cn2an==0.5.22
jieba==0.42.1
gradio==3.48.0
langid==1.1.6

# Note: numpy will be downgraded to 1.26.4 from 2.0.1 for librosa compatibility
